export enum ApiRoutes {
  Users = "/api/users",
  User = "/api/users/:id",
  Posts = "/api/posts",
  Post = "/api/posts/:id",
  Comments = "/api/comments",
  Comment = "/api/comments/:id",
}

export enum PageRoutes {
  Home = "/",
  Login = "/auth/login",
  Join = "/auth/join",
  Comments = "/comments",
}
