import { BaseLayout } from "@/app/layouts";
import LoginCard from "./login-card";

const LoginPage = () => {
  return (
    <BaseLayout>
      <LoginCard />
    </BaseLayout>
  );
};

export default LoginPage;
